# Backend API
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# ElevenLabs — server-side (for signed URL flow when agent auth is enabled)
ELEVENLABS_API_KEY=
ELEVENLABS_AGENT_ID=

# ElevenLabs — client-side (for public agent direct connection, no signed URL needed)
NEXT_PUBLIC_ELEVENLABS_AGENT_ID=
