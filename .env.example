# ================================
# CultureLens Environment Variables
# ================================
# copy this file to .env and fill in the values

# --------------------------------
# Backend API
# --------------------------------
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# --------------------------------
# ElevenLabs Configuration
# --------------------------------
# get your API key from: https://elevenlabs.io/app/settings/api-keys
# get your agent ID from: https://elevenlabs.io/app/conversational-ai

# server-side (for private agents requiring authentication)
ELEVENLABS_API_KEY=sk_your_api_key_here
ELEVENLABS_AGENT_ID=agent_your_agent_id_here

# client-side (for public agents with direct connection, no signed URL needed)
NEXT_PUBLIC_ELEVENLABS_AGENT_ID=agent_your_agent_id_here

# --------------------------------
# Firebase Configuration
# --------------------------------
# get these values from: https://console.firebase.google.com
# navigate to: project settings → general → your apps → web app config
#
# if you don't have a web app yet:
# 1. click "add app" → select web icon (</>)
# 2. register your app with a nickname (e.g., "culturelens-web")
# 3. copy the firebaseConfig values below

NEXT_PUBLIC_FIREBASE_API_KEY=AIza...your_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789:web:your_app_id_here
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-YOUR_MEASUREMENT_ID

# --------------------------------
# Firebase Admin SDK (Server-Side)
# --------------------------------
# the admin SDK is used for server-side firebase operations with full admin access
# this is NOT committed to git - stored in firebase-adminsdk-key.json
#
# to get the admin SDK credentials:
# 1. go to: https://console.firebase.google.com/project/culturelens-2dd38/settings/serviceaccounts/adminsdk
# 2. click "generate new private key"
# 3. save the downloaded JSON file as: firebase-adminsdk-key.json
# 4. this file is already in .gitignore to prevent accidental commits
#
# note: this is DIFFERENT from the client SDK config above
# - client SDK: runs in browser, limited by security rules
# - admin SDK: runs on server, bypasses all security rules
